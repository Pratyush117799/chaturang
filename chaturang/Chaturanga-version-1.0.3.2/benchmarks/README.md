# Chaturanga Benchmark Vault

This directory is intended for storing exported benchmark libraries and historical data.

## Structure
- `libraries/`: Store exported `.txt` game libraries here.
- `reports/`: Store generated Markdown analysis reports here.

## Usage
When you finish a benchmark run in `benchmark.html`, you can download the library and save it here to build a historical database for the bot engine's performance.
